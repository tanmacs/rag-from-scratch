# rag-from-scratch
Built a Retrieval-Augmented Generation (RAG) pipeline from scratch in Python without LangChain or vector databases. Loads custom documents, chunks text, creates embeddings with sentence-transformers, retrieves with NumPy cosine similarity, and answers using a grounded local LLM.
